<% provide(:title, "login") %>
<h1>ログイン</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_with(scope: :session, url: login_path, local: true) do |f| %>

      <%= f.label :email, class: "label-login" %>
      <%= f.email_field :email, class: 'form-control' %>

      <%= f.label :password, class: "label-login" %>
      <%= f.password_field :password, class: 'form-control' %>
      
      <%= f.label :remember_me, class: "checkbox inline checkbox-login", for: "remember-me" do %>
        <%= f.check_box :remember_me, id: "remember-me" %>
        <span>パスワードを記憶しますか？</span>
      <% end %>

      <%= f.submit :login, class: "btn btn-primary btn-block btn-login" %>
    <% end %>
    <p>
      新規ユーザーですか？<%= link_to "アカウント登録はこちら", signup_path %>
    </p>
  </div>
</div>

<!--セッションには対応するSessionモデルが存在しないので-->
<!--@userは使えないし、インスタンス変数にあたるオブジェクトも無い-->
<!--form_with(model: @user, local: true)は使えない-->
<!--リソース名（session）と対応するルーティングを具体的に指定するには、-->
<!--form_with(scope: :session, url: login_path, local: true)を使う。-->